import './App.css';
import WordCard from './WordCard';
import countDown from './Timedown';
const word = "kub";
function App() {
    return (
      <div>
        
          Timedown(15);
          <WordCard value = {'kub'}/>
  
      </div>
    );
}

export default App;
